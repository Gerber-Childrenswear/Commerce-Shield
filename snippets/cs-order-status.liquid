{% comment %}
  Commerce Shield — Order Status / Thank-You Page

  POLICY:
    A visitor reaching the order status / thank-you page has completed a real
    purchase and is treated as a confirmed human. We do NOT load the pixel
    guard here. All conversion pixels (GA4, Meta, etc.) MUST fire normally
    so revenue attribution is preserved.

    This snippet is intentionally empty. It exists as a placeholder so the
    "Additional scripts" textarea documents the policy and so future agents
    know not to add suppression logic to the thank-you page.

  WHERE THIS LIVES:
    Shopify Admin → Settings → Checkout → "Order status page" →
    "Additional scripts" textarea.
{% endcomment %}

<!-- Commerce Shield: thank-you page is unsuppressed by design (purchaser = confirmed human) -->
